package model.cards;

import model.Grid;
import model.Player;

public interface Card {
      //void applyEffect(Grid grid, Player p, Player p1); //player attacks p1   (useful? almost every card has a different signature)

}
